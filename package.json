{
  "name": "intercept-advantage-blog",
  "private": true,
  "scripts": {
    "build": "tinacms build && eleventy",
    "dev": "tinacms dev -c \"eleventy --serve\"",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@tinacms/cli": "^1.5.0",
    "tinacms": "^2.2.0"
  }
}
